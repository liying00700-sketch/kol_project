# 星链红人增长引擎 PRD

版本：v1.2  
日期：2026-07-16  
产品定位：以 StarAgent 为统一入口的 AI 驱动红人营销智能系统  
目标客户：消费品牌、出海品牌、内容电商团队、品牌代理商

---

## 1. 产品概述

### 1.1 产品定义

星链红人增长引擎不是传统红人管理 SaaS，也不是通用聊天机器人，而是一套以 StarAgent 为统一交互入口、以业务对象和证据链为基础、以任务执行闭环为目标的 AI 红人营销智能系统。

业务、推广、红人运营和管理者既可以进入经营驾驶舱、红人资产、内容资产等专业模块操作，也可以直接向 StarAgent 表达业务目标。StarAgent 负责理解意图、补齐必要条件、调用系统数据与专业 Agent、生成可解释方案，并将结果带入业务页面或转为待确认动作。

```text
自然语言业务目标
→ 意图识别与必要追问
→ 业务对象与约束结构化
→ 数据查询与专业 Agent 编排
→ 红人/内容/产品/场景/预算组合建议
→ 推荐理由、证据、限制与风险
→ 进入操作中心或生成 Campaign 草稿
→ 人工确认建联及其他商业动作
→ 执行反馈与复盘学习
```

系统核心价值：

- 让不熟悉系统结构的业务人员用一句话发起红人营销任务。
- 将“找红人、找素材、做方案、看风险、发起执行”收敛为统一入口。
- 判断红人营销预算是否有效，并提出预算调整建议。
- 识别产品、场景、红人、内容、广告和 VOC 的高价值组合。
- 为每项 AI 推荐提供可追溯的数据证据链。
- 将建议直接转为可编辑、可确认、可追踪的业务动作。
- 通过执行结果与人工反馈持续校准推荐。

### 1.2 产品目标

1. 用户能够用自然语言在 3 分钟内获得一份可执行的红人营销候选方案。
2. 用户能够看到红人、素材、产品、场景和预算的组合建议及推荐理由。
3. 用户能够区分数据事实、模型判断和业务假设。
4. 用户能够从 StarAgent 结果一键进入带筛选条件的专业页面。
5. 用户能够生成 Campaign、Brief 和建联计划草稿。
6. 所有对外发送、报价、合同、付款、授权、投放等商业动作必须人工确认。
7. 系统能够读取执行结果并改进后续建议。

### 1.3 非目标

- 不做无业务对象、无证据、无动作闭环的通用聊天助手。
- 不承诺在数据不足时给出确定性结论。
- 不做完全自动化的红人邀约、报价、合同、付款和广告发布。
- 不替代专业页面中的复杂数据分析和批量精细操作。
- MVP 不实现开放互联网的自主搜索与无限工具调用。
- MVP 不做全平台实时爬虫、复杂实时归因和大规模模型训练。

### 1.4 产品原则

#### 1.4.1 先理解任务，再给结果

StarAgent 必须识别产品、目标、预算、市场、平台、周期、受众和约束。缺少会实质影响结果的信息时，只追问必要问题；可采用默认值时，必须显式展示假设并允许修改。

#### 1.4.2 结果必须落到业务对象

每次对话至少关联一个 Product、Scene、Influencer、Content、Campaign、VOC 或 BudgetPlan，不生成脱离业务对象的泛泛答案。

#### 1.4.3 推荐必须可解释

每项建议必须说明：

- 使用的数据来源和时间范围。
- 样本规模和数据新鲜度。
- 核心匹配指标。
- 证据强度和归因强度。
- 模型推断、业务假设与已知事实的边界。
- 风险、限制和替代方案。

#### 1.4.4 输出必须可操作

Agent 输出必须支持继续筛选、替换对象、调整预算、生成 Campaign、生成 Brief、创建动作或生成建联计划。

#### 1.4.5 高风险动作人工确认

StarAgent 可以准备草稿和执行计划，但不能越权实施商业动作。

---

## 2. 用户与核心场景

### 2.1 目标用户

| 角色 | 核心诉求 | StarAgent 典型任务 |
|---|---|---|
| CMO / 品牌负责人 | 判断预算效率和投放方向 | “总结本月表现并给出下月预算建议” |
| 增长负责人 | 提升增长效率 | “找出 CAC 上升的原因和可调整组合” |
| 推广 / 业务 | 快速找资源并启动项目 | “1 万预算推广吸奶器，目标传播，给我一套方案” |
| 红人运营 | 选人、复投、建联和跟进 | “找 20 个适合职场妈妈场景且可授权的红人” |
| 内容运营 | 找素材、结构和 Brief | “找近期可复用的吸奶器素材并生成 Brief” |
| 广告投放 | 找可放大素材 | “找自然表现好且授权可用的短视频” |
| 产品团队 | 从 VOC 找机会 | “用户对便携吸奶器最大的抱怨是什么” |
| 数据团队 | 保证推荐可信 | “查看本次推荐的数据来源和评分版本” |

### 2.2 核心任务类型

一期支持以下任务：

1. 找红人：按照产品、场景、目标、预算和风险约束推荐个人或组合。
2. 找素材：寻找可复用、可授权、可放大或适合参考的内容。
3. 做推广方案：生成红人结构、内容方向、预算拆分和预期指标。
4. 创建 Campaign：将推荐结果转为可编辑的 Campaign 草稿。
5. 生成建联计划：生成候选名单、邀约话术、Brief 和跟进任务。
6. 查询经营问题：回答预算、效果、风险、机会与异常问题。
7. 查找资源：定位产品、红人、内容、报告、任务等系统对象。

### 2.3 示例场景

用户输入：

> 我想投放一个吸奶器，预算 1 万，主要目标是传播，不是转化。

StarAgent 应完成：

1. 提取产品、预算和目标。
2. 检查市场、平台、周期和目标人群是否已有品牌默认配置。
3. 对实质缺失的信息发起最少追问，或明确采用的默认假设。
4. 将传播目标转换为触达、有效播放、互动、收藏、品牌搜索提升、素材沉淀等指标，而非以 GMV 排序。
5. 推荐红人角色组合，例如专业教育型、职场妈妈场景型和真实体验 KOC。
6. 给出红人、内容结构、场景、预算和预留费用组合。
7. 展示每个推荐的证据、风险和替代对象。
8. 支持打开已带条件的红人资产/内容资产页面。
9. 支持生成 Campaign 草稿或待确认建联计划。

### 2.4 关键成功标准

- 80% 的一期高频任务可以从自然语言进入正确业务流程。
- 结构化条件识别准确率达到 90%。
- 推荐结果 100% 包含证据入口和数据限制。
- 用户可在不重复输入条件的情况下进入专业页面继续操作。
- 建联计划生成后，所有对外发送动作均处于待人工确认状态。
- 用户对推荐的采纳、替换和拒绝原因可以回流系统。

---

## 3. 产品信息架构

```text
StarAgent 智能工作台
──────────────────
经营驾驶舱
产品与场景
红人资产
内容资产
广告放大
VOC 与机会
Campaign 复盘
动作中心
数据与模型
系统设置
```

除一级导航外，系统顶部常驻全局快捷入口：

```text
问 StarAgent：找红人、找素材、做方案或分析数据……
```

### 3.1 双入口原则

- StarAgent：适合目标明确但不熟悉系统路径、需要跨模块组合的用户。
- 专业页面：适合精细筛选、批量操作、审计和深度分析。
- 两种入口共享相同对象、指标、权限、证据和动作状态，不形成两套系统。

---

## 4. StarAgent 智能工作台

### 4.1 页面目标

让用户以业务语言表达目标，由系统完成条件结构化、数据检索、Agent 编排、方案生成和后续动作衔接。

### 4.2 页面结构

| 区域 | 内容 |
|---|---|
| 对话区 | 用户输入、Agent 追问、进度和结论摘要 |
| 任务理解区 | 产品、目标、预算、市场、平台、周期、受众、限制和假设 |
| 方案画布 | 红人组合、素材组合、场景、预算分配、预期指标 |
| 证据区 | 数据来源、指标、样本、时间范围、强度、限制和风险 |
| 操作区 | 调整、替换、打开专业页面、生成 Campaign、生成建联计划 |
| 历史任务区 | 历史会话、版本、采纳状态和执行结果 |

### 4.3 意图与参数

| 参数 | 是否必需 | 说明 |
|---|---:|---|
| task_type | 是 | 找红人、找素材、做方案、查询、创建任务 |
| product | 是 | 已有产品或临时产品描述 |
| objective | 是 | 传播、教育、种草、转化、素材沉淀等 |
| budget | 条件必需 | 总预算、币种及是否含样品/物流/授权/加热 |
| market | 条件必需 | 国家或区域 |
| platform | 可选 | 可由目标和历史数据推荐 |
| audience | 可选 | 目标人群 |
| time_range | 可选 | 投放周期和数据观察范围 |
| constraints | 可选 | 粉丝量级、竞品、授权、合规和排除条件 |

### 4.4 必要追问规则

- 信息缺失会改变候选集合或预算可行性时必须追问。
- 已有品牌/用户默认值时优先复用，但必须展示。
- 单轮最多优先询问 1—3 个高影响问题。
- 用户可选择“按建议默认值继续”。
- 不得通过连续追问阻塞简单检索任务。

### 4.5 推荐方案要求

方案至少包含：

- 任务摘要和优化目标。
- 推荐策略与红人角色结构。
- 候选红人及备选红人。
- 推荐素材或内容结构。
- 产品卖点与用户场景。
- 预算分配与费用口径。
- 预期指标和观察窗口。
- 证据强度、风险和限制。
- 建议下一步。

传播目标下，排序权重应优先考虑有效触达、播放质量、互动质量、目标人群匹配、场景表达、品牌安全和素材沉淀，而不是短期转化。

### 4.6 结果编辑

用户可以：

- 修改预算、目标、市场、平台和周期。
- 锁定、移除或替换某个红人/素材。
- 调整不同红人角色的预算比例。
- 切换保守、均衡、激进方案。
- 比较方案的覆盖、风险、证据和预期影响。
- 保存方案版本并添加人工备注。

### 4.7 带条件跳转

StarAgent 必须能生成可复现的结构化筛选条件，并跳转至：

- 红人资产：展示匹配候选和筛选条件。
- 内容资产：展示推荐素材和内容结构。
- 产品与场景：展示使用的场景和卖点。
- Campaign：打开已预填的活动草稿。
- 动作中心：打开生成的建联或验证任务。

### 4.8 证据链

每条候选推荐需展示：

| 维度 | 示例 |
|---|---|
| 事实 | 近 90 天母婴内容 12 条，平均有效播放 8.6 万 |
| 推断 | 适合承担职场背奶场景的传播角色 |
| 匹配依据 | 受众、场景、表达、信任、成本和风险评分 |
| 数据范围 | 平台、时间、样本量、更新时间 |
| 强度 | 高、中、低及原因 |
| 限制 | 缺少授权历史或受众数据来自估算 |
| 风险 | 竞品合作、履约、舆情、刷量或合规风险 |

### 4.9 异常与降级

- 数据不足：返回“探索性候选”，降低证据强度并建议小规模测试。
- 无满足预算的红人：建议调整红人结构、平台、数量或预算。
- 条件冲突：指出冲突项并给出最小修改建议。
- 数据过期：标明更新时间，不用旧数据包装为当前事实。
- Agent 失败：保留已提取条件，允许进入专业页面手动筛选。

---

## 5. 核心专业模块

### 5.1 经营驾驶舱

展示预算、有效预算、浪费预算、收入、新客、CAC、ROI、ROAS、五维增长、机会风险和 AI 建议。所有异常和建议均可“交给 StarAgent 分析”，并携带当前对象和时间上下文。

### 5.2 产品与场景

管理产品生命周期、卖点、用户痛点、情绪驱动、信任证据、适合平台/形式及验证状态。支持让 StarAgent 基于当前产品生成场景、找红人或做方案。

### 5.3 红人资产

红人评分由产品匹配、场景匹配、受众匹配、内容表达、品牌信任、传播/转化贡献、授权价值、履约稳定和风险扣分组成。列表支持读取 StarAgent 生成的筛选方案；详情页展示匹配解释、历史合作、关联内容、VOC、风险和下一步动作。

### 5.4 内容资产

管理内容从 Brief、发布、自然验证、授权、加热、广告放大到结构沉淀的全过程。支持 StarAgent 搜索素材、比较内容结构并生成 Brief 草稿。

### 5.5 广告放大

识别可授权、可加热、可二创的素材，跟踪素材疲劳、授权期限与广告效率。StarAgent 可以推荐广告候选，但发布和预算调整必须确认。

### 5.6 VOC 与机会

聚类评论与用户反馈，识别产品、内容、品牌和风险主题；可将主题交给 StarAgent 转化为推广方案或验证任务。

### 5.7 Campaign 复盘

解释产品、场景、红人、内容、广告和用户触点的贡献，区分归因强度，生成下一轮策略并将结果反哺 StarAgent。

### 5.8 动作中心

承接系统、人工、专业 Agent 和 StarAgent 生成的任务。

```text
待确认 → 待指派 → 待执行 → 执行中 → 待验证 → 已完成 → 已复盘
```

动作字段包括来源会话、来源方案、目标对象、负责人、截止时间、证据摘要、预期影响、审批状态、执行结果和反馈。

---

## 6. Campaign 与一键建联

### 6.1 Campaign 草稿

StarAgent 可生成包含目标、预算、平台、周期、产品、场景、红人组合、内容方向、指标和风险的 Campaign 草稿。用户确认前不占用真实预算、不发送信息。

### 6.2 一键建联定义

产品中的“一键建联”定义为：

```text
一键生成建联计划
→ 自动生成候选名单、差异化话术、Brief 和跟进节点
→ 风险与联系方式检查
→ 人工预览和确认
→ 批量发送
→ 跟踪回复、报价和后续状态
```

### 6.3 必须人工确认的动作

- 发送红人邀约或其他外部消息。
- 确认报价、合同和付款。
- 授权内容使用。
- 发布广告和调整大额预算。
- 下架内容或发布风险声明。
- 对外共享包含敏感经营数据的报告。

---

## 7. Agent 体系

### 7.1 Agent 分层

- StarAgent：理解任务、维护会话状态、编排专业 Agent、组织结果和发起动作。
- 专业 Agent：产品场景策略、红人匹配、Brief、内容结构、VOC、广告放大、Campaign 复盘、合规风险。
- 证据与动作能力：统一构建证据、权限检查、审批和反馈闭环。

StarAgent 不替代专业 Agent，而是统一入口与编排层。

### 7.2 标准输出

所有 Agent 输出必须包括：结论、建议、关联对象、证据、证据强度、归因强度、假设、限制、风险、预期影响和可执行动作。

### 7.3 反馈学习

记录采纳、拒绝、替换、预算调整、建联回复、合作结果、内容表现和复投结果。用户拒绝推荐时可以选择原因：预算不合适、风格不匹配、数据不可信、已有合作冲突、风险过高或其他。

---

## 8. 权限、隐私与审计

- StarAgent 只能读取当前用户有权限访问的数据。
- 结果不得泄露跨品牌、跨租户的明细数据。
- 对外发送前重新执行权限、合规和风险检查。
- 必须记录用户输入、结构化任务、查询条件、数据版本、Agent/规则版本、输出、审批和执行事件。
- 敏感字段在日志和模型上下文中按权限脱敏。

---

## 9. 核心数据对象

在原有 Brand、Product、Scene、Influencer、Content、Campaign、Action、AgentOutput、AgentEvidence 等对象上新增：

| 对象 | 说明 |
|---|---|
| AgentConversation | 一次持续对话及其用户、租户和状态 |
| AgentMessage | 用户、Agent、系统或工具消息 |
| AgentTask | 从对话提取的结构化业务任务 |
| TaskConstraint | 预算、平台、市场、周期和排除条件 |
| RecommendationPlan | 可版本化的组合推荐方案 |
| RecommendationItem | 红人、内容、场景、产品或预算项 |
| EvidenceSnapshot | 推荐生成时使用的证据快照 |
| HandoffContext | 跳转专业页面时携带的条件和对象 |
| OutreachPlan | 建联候选、话术、Brief、跟进和审批状态 |
| ApprovalRequest | 对外动作或高风险动作的确认记录 |

---

## 10. 指标体系

### 10.1 产品指标

- StarAgent 周活跃用户数。
- 任务完成率和追问后流失率。
- 首次有效方案生成时长。
- 专业页面跳转率。
- Campaign/建联计划生成率。
- 推荐采纳率、替换率和拒绝率。
- 建联确认率、回复率和合作转化率。

### 10.2 推荐质量指标

- 意图识别准确率。
- 参数抽取准确率。
- 证据覆盖率。
- 无依据推荐率。
- 用户人工调整幅度。
- 推荐红人的实际表现相对基线提升。
- 不同角色、平台和预算段的偏差与公平性。

---

## 11. 版本范围与优先级

### 11.1 P0：可演示 MVP

- StarAgent 一级入口和全局快捷输入框。
- 找红人、找素材、做方案、查询机会/风险四类意图。
- 3—5 个完整产品演示数据集。
- 规则与 mock 数据驱动的必要追问和方案生成。
- 红人/素材/预算组合卡、证据抽屉和方案编辑。
- 带条件跳转红人资产和内容资产。
- 生成 Campaign 草稿及待确认建联任务。
- 明确展示人工确认边界。

### 11.2 P1：客户试点

- 真实数据库和客户数据导入。
- 服务端任务解析、检索、评分和规则 Agent。
- 会话、方案版本、证据快照和反馈持久化。
- 真实 Campaign/动作中心衔接。
- 权限、审批、审计和基础消息渠道接入。
- 离线评估集和推荐质量看板。

### 11.3 P2：商业化版本

- 多租户与更多外部数据源。
- LLM 驱动的 StarAgent 编排。
- 多方案模拟和高级预算优化。
- 多渠道建联与回复状态同步。
- Agent 版本管理、实验与持续评估。
- 在严格权限和审批下扩展更多工具。

---

## 12. P0 验收标准

1. 用户输入“1 万元推广吸奶器，以传播为目标”，系统能正确提取产品、预算和目标。
2. 系统能追问或明确市场、平台、周期等关键假设。
3. 系统返回至少一套红人、素材、场景和预算组合方案。
4. 传播目标下不以短期转化作为主要排序依据。
5. 每个推荐项均有推荐理由、证据入口、强度、风险和限制。
6. 用户可以替换候选、调整预算并保存新版本。
7. 用户可以跳转到带筛选条件的红人资产或内容资产页面。
8. 用户可以生成 Campaign 草稿和建联计划。
9. 点击发送邀约前必须出现人工确认，不允许 Agent 自动外发。
10. 用户反馈和最终动作状态可以回到原会话和推荐方案。

---

## 13. Demo 改造顺序

1. 增加 StarAgent 一级导航和全局快捷入口。
2. 建立演示会话、任务、方案、证据和建联数据。
3. 实现任务理解卡和必要追问交互。
4. 实现组合方案画布和传播目标排序规则。
5. 实现证据抽屉、风险与限制展示。
6. 实现方案调整和候选替换。
7. 实现带条件跳转红人资产/内容资产。
8. 实现 Campaign 草稿与建联待确认动作。
9. 串联经营驾驶舱、产品场景、VOC 和复盘的“交给 StarAgent”入口。
10. 完善权限、审计和反馈演示。

